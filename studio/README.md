# RailSight MVP

This is a Next.js application for RailSight, designed to manage railway cable assets.

It includes role-based UIs for Admins, Supervisors, and Field Officers.

To get started, run the development server and navigate to the homepage to select a user role.
```
npm run dev
```

The application will be accessible at `http://localhost:9002`.
